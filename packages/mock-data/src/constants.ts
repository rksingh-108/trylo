import type { GeoPoint } from "@trylo/types";

/** Default map center (Bengaluru-style coordinates), used as the placeholder "current location". */
export const CITY_CENTER: GeoPoint = { lat: 12.9716, lng: 77.5946 };
