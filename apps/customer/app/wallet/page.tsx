import { RouteStub } from "@/components/route-stub";

export default function WalletPage() {
  return <RouteStub checkpoint="Checkpoint 4" title="Wallet" description="Balance, add money, transaction history." />;
}
