import { RouteStub } from "@/components/route-stub";

export default function HistoryPage() {
  return (
    <RouteStub checkpoint="Checkpoint 4" title="Ride history" description="Past rides list, ride detail view, receipt." />
  );
}
