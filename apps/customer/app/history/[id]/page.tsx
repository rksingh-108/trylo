import { RouteStub } from "@/components/route-stub";

export default function RideDetailPage() {
  return <RouteStub checkpoint="Checkpoint 4" title="Ride detail" description="Receipt and trip detail for a past ride." />;
}
