import { RouteStub } from "@/components/route-stub";

export default function ProfilePage() {
  return (
    <RouteStub checkpoint="Checkpoint 4" title="Profile" description="Vehicle info, documents status, ratings, support." />
  );
}
